
namespace Course
{
    struct Point // Exemplo de "Struct". Bem parecido com a criação de classe.
    {
        public double X;
        public double Y;

        public override string ToString()
        {
            return "( " + X + " , " + Y + ")";
;        }
    }
}
